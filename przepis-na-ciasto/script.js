/* /* let myWord = 'Akademia108';

let newArray = myWord.split('');
console.log(newArray);

let reversedArray = newArray.reverse();

console.log(reversedArray);

let combinedReversed = reversedArray.join(''); */

/* let hisStuff = [1, 'lion', 'sun', 'son', 5, 15, 'zip', 'New York', 45, 85, false, true, 'no'];
let myStuff = [1, 'bird', 'sun', 'json', 5, 15, 'zip', 'Boston', 31, 85, 'crimson', 'no'];

let mutualStuff = [];

for (let i = 0; i < hisStuff.length; i++) {
    for (let j = 0; j < myStuff.length; j++) {
        if (hisStuff[i] === myStuff[j]) {
            mutualStuff.push(hisStuff[i]);
        }
    }
};
console.log(myStuff.length); */

/* let item;

while (item != 'Boston') {
    item = myStuff[Math.floor(Math.random()*10)];
    console.log(item);
  } */

/* let number = 1;

while (number != 3) {
    number = Math.floor(Math.random()*10);
    console.log(number);
};
 */
/* for (k = 1; k < 10; k++) {
 console.log(Math.round((Math.random()*10)+k));
} */
/* let list = document.getElementById('list');

console.log(list);

for (let listItem of list.children) {
    listItem.setAttribute('class', 'listItem');
};
 */
//odwolanie do zmiennej w backtickach: console.log(`Moj pies wabi sie ${dogsName}`)

/* loop: for (i = 0; i<anArray.length; i++) {
    console.log(`something something ${i}`);
} */

/*loop: for in 

let car = {
    brand: 'Toyota',
    model: 'Corolla',
    year: '2020'
};

for (let key in car) {
    console.log(`Nazwa pola: ${key}, wartosc pola: ${car[key]}`);
} */

/* let names = [ 'Krystian',  'Monika',  'Danuta' ];
 
names.forEach( function( element, index ) {
     console.log( 'Element z Indexem: ' + index + ' ma wartość ' + element );
}); */